#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string followed by a new line
 * @str: The string to be printed
 */
void _puts(char *str)
{
    /* Placeholder statement to use the 'str' parameter */
    printf("%s\n", str);
}
